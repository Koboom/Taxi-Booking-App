version: '3.8'
services:
  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend

  backend:
    build: ./backend
    environment:
      MONGODB_CONNECTION_STRING: "mongodb://mongodb/taxi-booking"
    ports:
      - "3000:3000"
    depends_on:
      - mongodb

  mongodb:
    image: mongo:4.2
    ports:
      - "47017:27017"
    volumes:
      - ./data:/data/db
